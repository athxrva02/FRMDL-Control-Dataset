# Pinned to a working set captured after install on macOS arm64 (Python 3.11).
# NumPy is held < 2 because the 2019-era imagecorruptions / scikit-image stack
# relies on APIs and dtype aliases removed in NumPy 2.x (see README "Compatibility").
numpy==1.26.4
scipy==1.17.1
pandas==3.0.3
matplotlib==3.10.9
pillow==12.2.0
tqdm==4.67.3
pyyaml==6.0.3
scikit-image==0.26.0
opencv-python==4.11.0.86
torch==2.12.0
torchvision==0.27.0

# imagecorruptions packages Hendrycks & Dietterich's original ImageNet-C code.
# Installed from source (not the PyPI wheel) so the bundled `frost` overlay
# images are present; the plain wheel ships without them and `frost` fails.
imagecorruptions @ git+https://github.com/bethgelab/imagecorruptions.git
