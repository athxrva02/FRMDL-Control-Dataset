# Environment
.venv/
__pycache__/
*.pyc

# Generated dataset and raw download (reproducible from generate.py + seed + manifest.csv).
data/imagenette2-320/
data/imagenette2-320.tgz
data/base/
data/clean_saves/
data/corrupted/
data/imagenet_class_index.json

# Results are regenerated by evaluate.py / analyze.py. Keep CSVs out of git by default;
# remove these lines if you want to commit a results snapshot.
results/predictions.csv

# Manifest IS committed (rubric requirement) — do not ignore manifest.csv.

.DS_Store

# Logs
logs/
